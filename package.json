{
  "name": "nau-platform",
  "version": "0.0.0",
  "private": true,
  "description": "naŭ Platform — shared packages root",
  "scripts": {
    "build": "pnpm -r --filter './packages/**' build",
    "typecheck": "pnpm -r --filter './packages/**' typecheck",
    "dev": "pnpm -r --filter './packages/**' dev"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^24.1.0"
  }
}
