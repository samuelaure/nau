{
  "name": "nau-platform",
  "version": "0.0.0",
  "private": true,
  "description": "naŭ Platform monorepo",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "build:packages": "pnpm -r --filter './packages/**' build"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3",
    "@types/node": "^24.1.0"
  }
}
