# ⚠️ Superseded

This document has been replaced. The model is now called `Post` (not `ContentPiece`) and is being implemented in the current sprint. See `post-frequency-feature.md` and `composer-formats.md` for the planned follow-on features that build on top of it.
