// Cypress.Commands.add('login', (username, password) => {
//   // Custom command to handle a login flow.
//   // This is a placeholder as the app does not have a login feature yet.
//   // We can use it to set a token in local storage or session storage
//   // to bypass the login UI in future tests.
//   window.localStorage.setItem('auth_token', 'mock_token')
// })
