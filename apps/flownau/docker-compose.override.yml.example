version: '3.8'

services:
  # In production, we don't spawn a local DB but join the mesh
  postgres:
    profiles:
      - production_disabled
  
  redis:
    profiles:
      - production_disabled

networks:
  default:
    external:
      name: shared-mesh
