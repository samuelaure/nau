/**
 * `(Journal)·(Time)` — DRAFT, see `contract.ts`.
 *
 * Published at `@nau/journal/relations/time`, following the same subpath
 * pattern as `relations/gtd/` (nau#115): the relation stays optional, and
 * whoever confirms it decides whether `apps/api` should import the type
 * from here instead of keeping its own copy.
 */
export * from './contract';
