/**
 * `(References)·(Actions)` — the aggregate/elevation contract References
 * publishes toward Actions, as `@nau/references/relations/actions`. Same
 * discipline as `relations/gtd`: this relation is optional, and deleting the
 * folder removes it with no change to the rest of the package.
 */
export * from './contract';
