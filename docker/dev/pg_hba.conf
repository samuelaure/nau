# pg_hba.conf for local development
# Trust all connections — safe because ports are only bound to localhost.
local   all   all              trust
host    all   all   0.0.0.0/0  trust
host    all   all   ::/0       trust
